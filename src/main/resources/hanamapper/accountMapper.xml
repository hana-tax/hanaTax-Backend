<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.domain.account.mapper.AccountMapper">

    <!-- select 문으로 리스트 조회 -->
    <select id="getAccountList" parameterType="com.example.demo.domain.account.dto.DepositAccountListRequest" resultType="com.example.demo.domain.account.dto.DepositAccountListResponse">
        SELECT
            depositAccountId,
            accountPasswd,
            accountStatus,
            balance,
            createDate,
            expirationDate,
            depositId,
            id,
            accountType
        FROM
            deposit_account
        WHERE
            id = #{id}
    </select>

</mapper>
